import datetime
from datetime import datetime

monday = 0
months = range(1,3)
	for year in range(2015,2017):
		for month in months:
			if datetime(year,month,1).weekday()==0:
			monday += 1
print(monday1)

